PhotoSync ver.{{VERSION_NAME}}  (versionCode {{VERSION_CODE}})

本次更新（Android）：
- 修复：增量同步不再在部分场景下自动升级为全量扫描，避免“增量看起来和全量一样/耗时接近全量”。
- 新增：扫描阶段进度上报。扫描 MediaStore 时会输出“扫描进度 x/y”，并同步上报到电脑端供网页实时显示。

提示：
- 如果你是 USB(ADB) 连接：客户端内部仍走 localhost 映射，但界面不会再强行显示 localhost。

本次更新（电脑端 / Web）：
- 修复：跳过判定统一为“数据库命中且磁盘文件存在”。当数据库记录存在但磁盘文件缺失时，会自动清理无效索引，避免误判为已存在。
- 新增：跳过列表记录命中原因。当命中的电脑端文件名与手机端文件名不一致时，显示“(已存在: 电脑端文件名)”。
