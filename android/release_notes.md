PhotoSync ver.{{VERSION_NAME}}  (versionCode {{VERSION_CODE}})

本次更新（Android）：
- 深色/浅色一键切换（左上角按钮），并记住你的选择。
- 深色模式下输入框/文本颜色跟随主题，不再出现“白色输入框突兀”。
- 服务器地址默认值优化：首次默认填入 192.168.1.7，并会记住你后续改的 IP/端口。
- GitHub 图标夜间自动反色（不加白底外壳），在深色背景更清晰。

提示：
- 如果你是 USB(ADB) 连接：客户端内部仍走 localhost 映射，但界面不会再强行显示 localhost。
